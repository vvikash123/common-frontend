import React from "react";
import style from "./InsightExperts.module.scss";

const InsightExperts = (props) => {
  return (
    <div className={style["tnn_container"]}>
      <h2> InsightExperts</h2>
    </div>
  );
};

export default InsightExperts;
